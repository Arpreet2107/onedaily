name: keploy-test

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      mongo:
        image: mongo
        ports:
          - 27017:27017

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install Keploy CLI
        run: curl -s https://keploy.io/install.sh | bash

      - name: Install backend dependencies
        working-directory: ./onelinedaily-backend
        run: npm install

      - name: Start Backend and Run Keploy Tests
        working-directory: ./onelinedaily-backend
        run: |
          nohup npm start &
          sleep 10
          keploy test -c "npm start" -r true
